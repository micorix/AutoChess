<UserControl x:Class="AutoChess.Views.ChessBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:AutoChess.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <UserControl.Resources>
        <conv:PieceIconConverter x:Key="PieceIconConverter" />
        <conv:SquareBackgroundConverter x:Key="SquareBackgroundConverter" />
        <Style x:Key="SquareBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#282828" />
            <Setter Property="BorderThickness" Value="0.5" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Border BorderBrush="#484848" BorderThickness="1" CornerRadius="2"
                Background="#0a0a0a" Padding="2">
            <Grid>
                <ItemsControl ItemsSource="{Binding Squares, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="8" Columns="8" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource SquareBorderStyle}"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="Cell_Click">
                                <Border.Background>
                                    <MultiBinding Converter="{StaticResource SquareBackgroundConverter}">
                                        <Binding Path="IsSelected" />
                                        <Binding Path="IsLightSquare" />
                                    </MultiBinding>
                                </Border.Background>
                                <TextBlock Text="{Binding Piece, Converter={StaticResource PieceIconConverter}}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="42"
                                           FontWeight="Normal"
                                           Foreground="{Binding Foreground}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
